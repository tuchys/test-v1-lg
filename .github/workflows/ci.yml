# Dandela-managed workflow scaffold
# Dandela-template-version: SETUP-HARDEN-003-v1
# Dandela-managed-path: .github/workflows/ci.yml
name: ci
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI passed ✓"
