# Dandela-managed workflow scaffold
# Dandela-template-version: SETUP-HARDEN-003-v1
# Dandela-managed-path: .github/workflows/custom.yml
name: custom
# Template workflow — add your own steps below.
# Triggered manually via Cofabric run-actions or the GitHub Actions UI.
on:
  workflow_dispatch:
jobs:
  custom:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Custom workflow — replace this step with your own"
